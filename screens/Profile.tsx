import { StyleSheet, Text, View } from 'react-native';

const Profile = () => {
  return (
    <View style={styles.Container}>
      <Text>sananesananasdnberab s</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  Container: {},
});

export default Profile;
